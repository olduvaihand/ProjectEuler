/*
 * ProjectEuler/src/c/Problem372.c
 *
 * Pencils of rays
 * ===============
 * Published on Saturday, 18th February 2012, 01:00 pm
 *
 * Let R(M, N) be the number of lattice points (x, y) which satisfy MxN, MyN and
 * is odd.  We can verify that R(0, 100) = 3019 and R(100, 10000) = 29750422.
 * Find R(2·106, 109).    Note:  represents the floor function.
 */
 
#include <stdio.h>
#include "ProjectEuler/ProjectEuler.h"
#include "ProjectEuler/Problem372.h"


int main(int argc, char** argv) {

    return 0;
}